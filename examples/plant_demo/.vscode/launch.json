{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "structured-text",
            "request": "launch",
            "name": "Debug Structured Text",
            "program": "${workspaceFolder}/src/config.st",
            "stopOnEntry": false
        }
    ]
}
